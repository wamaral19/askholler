{
  "name": "@holler/worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@holler/domain": "*",
    "@holler/db": "*",
    "graphile-worker": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@holler/testkit": "*"
  }
}
